<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "zobacz swój profil", current_user %></span>
<h3>Zgłoszone lokalizacje (<%= current_user.locations.count %>)</h3>

